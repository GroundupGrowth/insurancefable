node_modules
.next
out
dist
dist-ssr
*.local
.env
.env.*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel
clipboard-*.png
.playwright-mcp/

# Raw Google Ads CSV exports — parsed into src/data/googleAdsData.ts by
# scripts/build-google-ads.mjs; the generated module is what ships.
/Google Ads/

# Raw CRM leads export — contains PII (names, emails, phones). NEVER commit.
# Parsed into aggregates-only src/data/leadsData.ts by scripts/build-leads.mjs.
/I&E Leads*.csv

# Full WordPress export of the live site (23 MB, includes unpublished drafts).
# Source material for content sync — never ship it in the repo.
*.WordPress.*.xml
