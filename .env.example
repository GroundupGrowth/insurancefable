# Supabase embed backend (see supabase/schema.sql). Copy to .env.local for dev;
# set the same two vars in Vercel -> Settings -> Environment Variables.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
